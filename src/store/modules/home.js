import {  } from "../../api/api"

const state = {
    rankList: []
}

const getters = {
    
}

const mutations = {
    
}

const actions = {
    
}

export default {
    state,
    getters,
    mutations,
    actions
}