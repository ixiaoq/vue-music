import {  } from "../../api/api"
import * as types from '../mutations_type'

const state = {
    // 当前音乐播放列表
    currentPlayMusicList: []
}

const getters = {
    
}

const mutations = {
    
}

const actions = {
    
}

export default {
    state,
    getters,
    mutations,
    actions
}